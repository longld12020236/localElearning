<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !current_user?(user) %>
    <span class="right"><%= link_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" } %></span>
  <% end %>
</li>
